/// Length-prefixed envelope: 4-byte big-endian length + JSON payload.
#[derive(Debug)]
pub struct Envelope {
    // Stub: fields TBD in implementation bead
}

/// Errors during envelope encoding/decoding.
#[derive(Debug)]
pub enum EnvelopeError {
    // Stub: variants TBD in implementation bead
}
