$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  check:
    command: 'cargo check -p wtf-api'
    toolchains:
      - rust
    options:
      runInCI: true

  fmt:
    command: 'cargo fmt -p wtf-api -- --check'
    toolchains:
      - rust
    options:
      runInCI: true

  lint-src:
    command: 'cargo clippy -p wtf-api --lib --bins --examples -- -D warnings -W clippy::all'
    toolchains:
      - rust
    options:
      runInCI: true

  test:
    command: 'cargo test -p wtf-api'
    toolchains:
      - rust
    options:
      runInCI: true
